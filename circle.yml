machine:
  python:
    version: 2.7.12
compile:
  override:
     - echo 'hello world'
     - echo $ABC
     - |
        CDE=${ABC}
        echo $CDE
     - FGH=${ABC}
     - echo $FGH
     - 'ls -l ~/'
     - 'aws --version'
    # - 'ls -l ~/.aws/'
deployment:
  dev:
    branch: dev
    commands:
       - echo 'hello deploy'
       - |
          AWS_ACCESS_KEY_ID=${DEV_AWS_KEY}
          AWS_SECRET_ACCESS_KEY=${DEV_AWS_SECRET}
          aws s3 sync ~/circlecitest/dist s3://s3awscddev1 --delete
test:
  override:
     - python nosetest.py
